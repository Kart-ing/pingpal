{
  "name": "pingpal-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "PingPal — ambient messaging for CLI coders (monorepo root)",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint"
  }
}
